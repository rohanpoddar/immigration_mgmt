<%= form_for @visa_type do |f| %>
    <% if @visa_type.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@visa_type.errors.count, "error") %> prohibited this Visa Type from being saved:</h2>

          <ul>
            <% @visa_type.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :country %>
      <%= f.text_field :country %>
    </div>

    <div class="action">
      <%= f.submit %>
    </div>
<%end%>
